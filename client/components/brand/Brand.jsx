import React from 'react';
import { Link } from 'react-router-dom';
import './brand.scss';

const Brand = () => (
  <div>
    <Link className="bucket-brand" to="/">Bucket-List</Link>
  </div>
);

export default Brand;
